// frontend/js/config.js
const CONFIG = {
    API_BASE_URL: window.location.origin,
    // If you need different URL for API, uncomment:
    // API_BASE_URL: window.location.hostname === 'localhost'
    //     ? 'http://localhost:5000'
    //     : window.location.origin
};
